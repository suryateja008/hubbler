create database hubbler;

CREATE TABLE Forms (FormID int NOT NULL AUTO_INCREMENT, form_fields blob NOT NULL, PRIMARY KEY (FormID));

sudo pip install -U flask-cors

sudo pip install mysql-connector

CREATE TABLE inputdata (FormID int, data blob NOT NULL);